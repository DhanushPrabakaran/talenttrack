"use client";
import React, { Suspense } from "react";
import SigninForm from "./_components/SigninForm";

const page = () => {
  <Suspense>
    <SigninForm />
  </Suspense>;
};

export default page;
